fn test_u8(a: u8, b: u8) -> u8 {
    const result: u8 = a + b;
    return result;
}

fn test_u16(a: u16, b: u16) -> u16 {
    const result: u16 = a + b;
    return result;
}

fn test_u32(a: u32, b: u32) -> u32 {
    const result: u32 = a + b;
    return result;
}